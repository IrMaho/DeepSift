These 1 image contain source code/text rendered as PNG pages by pxpipe.

Source files (1):
  deepsift.md

Instructions for the reading agent:
1. Read the images in order: page-001.png through page-001.png.
2. For any path, file name, identifier, hash, version number, or other precision-critical
   value: use the verbatim text from factsheet.txt, NOT what you read off the image.
   factsheet.txt is the authoritative source of truth for all exact strings.
3. Treat the content as if you had read the source files directly.

Factsheet (verbatim identifiers — quote from here, not from image pixels):
[Exact identifiers from the rendered context above (paths, ids, versions, numbers) — quote these verbatim instead of transcribing them from the image; ×N marks a token that occurs N times within the imaged content: --context-lines · --no-compress ×4 · --path-filter ×2 · --all-realms ×2 · excludeDirs · --include · --no-sync · --section ×2 · --verbose · --offset ×2 · --source ×2 · DeepSift ×18 · PageRank · --force · --limit ×2 · --query ×4 · --realm ×5 · L10-L15 ×2 · --meta ×2 · --show ×2 · --type ×2 · 0.45 · src/components/MyNewButton.tsx · skills/registry.json · src/core/indexer.ts · skills/README.md · /features/auth · /.../INDEX.md · src/file.ts]
